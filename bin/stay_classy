#!/usr/bin/env ruby
# A tool to add classes and ids to your html files
require 'stay_classy'

puts "Enter the prefix you want to use (e.g. foo for foo_div):"  
STDOUT.flush  
prefix = gets.chomp  
puts "Prefix:" + prefix  
# StayClassy.new(ARGV)